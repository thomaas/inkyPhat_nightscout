Pillow
font-intuitive
matplotlib
numpy
pydexcom
tconnectsync>=3.0,<4  # 3.0 switched to the JSON BFF API (get_pumper / get_pump_logs)
python-dotenv
setuptools<81
tzdata  # timezone database for zoneinfo (needed on minimal Linux installs)
# inky  # only on Raspberry Pi (GPIO/SPI required) — install separately there
