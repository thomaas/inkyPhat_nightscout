Pillow
font-intuitive
matplotlib
numpy
pydexcom
tconnectsync>=2.3.4  # older releases lack TandemSourceApi.pump_event_metadata()
python-dotenv
setuptools<81
tzdata  # timezone database for zoneinfo (needed on minimal Linux installs)
# inky  # only on Raspberry Pi (GPIO/SPI required) — install separately there
